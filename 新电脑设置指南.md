# 新电脑设置指南

> 另一台电脑上使用这个项目，只需 10 分钟。

---

## 1. 安装必要工具

### Node.js
- 打开 https://nodejs.org
- 下载 LTS 版本，双击安装，一路"下一步"
- 验证：打开终端，输入 `node -v`，显示版本号即成功

### Git
- 打开 https://git-scm.com/downloads
- 下载 Windows 版本，双击安装，一路"下一步"
- 验证：打开终端，输入 `git --version`，显示版本号即成功

### GitHub CLI（可选，推荐）
- 在终端执行：`winget install GitHub.cli`
- 登录：`gh auth login`
- 选"Login with a web browser"，按提示操作

### Claude Code
```bash
npm install -g @anthropic-ai/claude-code
```

---

## 2. 配置代理（如果需要 VPN）

```bash
# 查看你的 VPN 代理端口（Clash 一般是 789x）
git config --global http.proxy http://127.0.0.1:7897
git config --global https.proxy http://127.0.0.1:7897
```

---

## 3. 下载项目

```bash
cd Desktop
git clone https://github.com/ZhenyuJiao/cluade_code_sync.git
cd cluade_code_sync
```

---

## 4. 复制记忆文件

Claude Code 会把记忆存在项目路径对应的文件夹里。首次使用需要手动复制：

```bash
# 先启动一次 Claude Code（让它创建好目录结构）
claude
# 输入 /exit 退出

# 复制记忆文件
cp .claude/memory/* ~/.claude/projects/$(echo $(pwd) | sed 's/[:\/\\]/_/g')/memory/
```

> 如果提示目录不存在，先 `mkdir -p ~/.claude/projects/$(echo $(pwd) | sed 's/[:\/\\]/_/g')/memory/`

---

## 5. 启动

```bash
claude
```

项目里的 skills 和记忆都会自动加载，和另一台电脑一样用。

---

## 日常工作流程

```bash
cd Desktop/cluade_code_sync
git pull          # 拉取最新内容
claude            # 开始工作
# 工作完成后：
git add .
git commit -m "改了什么"
git push          # 推送到 GitHub
```

---

## 常见问题

**Q: git push 要密码？**
A: 装 `gh`（GitHub CLI）并 `gh auth login`，之后就不用输密码了。

**Q: 两台电脑同时改了怎么办？**
A: git push 时会被拒绝。先 `git pull` 合并代码，再推送。建议不要同时在一台以上电脑上工作。

**Q: 怎么看网站？**
A: 双击 `website/index.html`，浏览器直接打开。
