# 新电脑设置指南

> 用另一台电脑继续使用这个项目，只需 5 分钟。

## 第一步：安装必要工具

### 1. 安装 Node.js
- 打开 https://nodejs.org
- 下载 LTS 版本，双击安装，一路"下一步"
- 安装完成后，打开"终端"（cmd），输入 `node -v`，显示版本号即成功

### 2. 安装 Git
- 打开 https://git-scm.com/downloads
- 下载 Windows 版本，双击安装，一路"下一步"
- 安装完成后，打开"终端"，输入 `git --version`，显示版本号即成功

### 3. 安装 Claude Code
```bash
npm install -g @anthropic-ai/claude-code
```

### 4. 配置代理（如果你的网络需要 VPN 才能访问 GitHub）
```bash
# 以 Clash 端口 7897 为例，根据你的实际端口修改
git config --global http.proxy http://127.0.0.1:7897
git config --global https.proxy http://127.0.0.1:7897
```

---

## 第二步：下载项目

```bash
# 打开终端，cd 到你想放项目的位置（比如桌面）
cd Desktop

# 克隆项目
git clone https://github.com/ZhenyuJiao/cluade_code_sync.git

# 进入项目文件夹
cd cluade_code_sync

# 启动 Claude Code
claude
```

---

## 第三步：开始使用

Claude Code 启动后，会自动加载项目里的：
- **记忆文件** — 你的偏好设置和习惯
- **已安装的 skills** — find-skills、case-study-writer 等
- **项目配置** — 模型设置等

### 日常工作流程

```bash
cd Desktop/cluade_code_sync

# 先拉取最新代码（如果有其他电脑提交了更新）
git pull

# 用 Claude Code 工作
claude

# 工作完成后，把改动提交
git add .
git commit -m "更新内容"
git push
```

---

## 第四步：在另一台电脑上查看网站

直接用浏览器打开 `website/index.html` 就能看到案例网站。

如果要部署到网上让别人看，可以用 Vercel 或 GitHub Pages：
1. 在 GitHub 仓库页面点 Settings → Pages
2. 选择分支 `main`，文件夹 `/website`
3. 几分钟后就能通过 `https://你的用户名.github.io/cluade_code_sync` 访问

---

## 常见问题

**Q: git push 需要输入密码？**
A: 建议配置 SSH Key 或个人访问令牌（Token），网上搜"GitHub 配置 SSH Key"有详细教程。

**Q: 两边电脑改了冲突了怎么办？**
A: 建议不要同时在两台电脑上工作。如果冲突了，用 `git pull` 后会提示冲突文件，手动保留你要的版本即可。

**Q: 怎么更新案例？**
A: 编辑 `website/cases.json` 文件，添加新的案例对象，然后提交推送。
